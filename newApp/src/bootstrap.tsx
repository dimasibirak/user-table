import React from 'react';
import ReactDOM from 'react-dom';
import GeneralContainer from './containers/general-container';

const container = document.getElementById('root');
ReactDOM.render(<GeneralContainer />, container);